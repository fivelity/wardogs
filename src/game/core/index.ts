/**
 * game/core/index.ts — barrel re-export for every generic, reusable gameplay primitive.
 * Per BUILD_GUIDE.md §3: add one `export * from` line per new core primitive here. No logic.
 */
export * from "./transition-state.ts";
